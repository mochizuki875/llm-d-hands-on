apiVersion: v1
kind: Service
metadata:
  name: vllm-sim-decode-service
spec:
  selector:
    llm-d.ai/role: decode
  ports:
    - name: http
      port: 8000
      targetPort: sidecar
      protocol: TCP
  type: ClusterIP